query CategoriesGetAll($languageCode: LanguageCodeEnum!) {
	categories(first: 100) {
		edges {
			node {
				id
				name
				slug
				translation(languageCode: $languageCode) {
					name
				}
			}
		}
	}
}

